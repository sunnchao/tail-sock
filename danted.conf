logoutput: stderr
internal: 0.0.0.0 port = 1056
external: 127.0.0.1

clientmethod: none
socksmethod: username
user.privileged: root
user.unprivileged: nobody

# 默认允许任意来源连接到 danted，适合本机绑定或受防火墙保护的部署。
# 若将 1056 暴露到公网，请把 from 改成可信客户端网段。
client pass {
    from: 0.0.0.0/0 to: 0.0.0.0/0
}

# 认证通过后允许访问任意目标。可按需把 from/to 缩小到内网或 tailnet 范围。
socks pass {
    from: 0.0.0.0/0 to: 0.0.0.0/0
    socksmethod: username
}

# 关键：把认证后的流量转发给 tailscale 的上游 SOCKS5
route {
    from: 0.0.0.0/0 to: 0.0.0.0/0 via: 127.0.0.1 port = 1055
    proxyprotocol: socks_v5
}
